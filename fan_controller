#include "mbed.h"

// Assuming the temperature sensor is connected to PA0
AnalogIn tempSensor(PA_0);

// fan will be connected to PB0 digital pinout
DigitalOut fan(PB_0);

int main() {
    while(1) {
        fan(1);
    }
}


